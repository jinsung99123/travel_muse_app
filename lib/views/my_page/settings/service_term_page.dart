import 'package:flutter/material.dart';

class ServiceTermPage extends StatelessWidget {
  const ServiceTermPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('서비스 약관')),
      body: Text('서비스 약관'),
    );
  }
}
